require 'spec_helper'

describe YahooMarketCalendar do
  it 'has a version number' do
    expect(YahooMarketCalendar::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
